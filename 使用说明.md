# 纳斯达克量化交易系统 — 使用说明

## 目录

- [系统简介](#系统简介)
- [环境安装](#环境安装)
- [快速开始](#快速开始)
- [功能模块详解](#功能模块详解)
  - [1. 数据采集与处理](#1-数据采集与处理)
  - [2. 模型训练](#2-模型训练)
  - [3. 策略回测](#3-策略回测)
  - [4. 分析报告](#4-分析报告)
  - [5. 模型集成](#5-模型集成)
  - [6. 自主实验循环](#6-自主实验循环)
- [风控功能](#风控功能)
  - [个股止损](#个股止损)
  - [组合回撤熔断](#组合回撤熔断)
  - [滑点模拟](#滑点模拟)
- [周频调仓](#周频调仓)
- [完整参数列表](#完整参数列表)
- [输出文件说明](#输出文件说明)
- [常见问题](#常见问题)

---

## 系统简介

本系统是一个面向美股纳斯达克100指数成分股的量化交易系统，借鉴了 [autoresearch](https://github.com/karpathy/autoresearch) 的自主实验迭代理念和微软 [Qlib](https://github.com/microsoft/qlib) 的量化投资框架设计。

**核心能力：**

| 功能 | 说明 |
|------|------|
| 数据采集 | 自动从 Yahoo Finance 下载纳斯达克100成分股行情数据 |
| 因子工程 | 内置 Alpha158 因子体系（142个特征因子） |
| 模型训练 | 支持 LightGBM、Ridge、LSTM、GRU 四种模型 |
| 模型集成 | 支持多模型加权集成（等权/IC加权/排名平均） |
| 策略回测 | TopK-Dropout 选股策略，含风控与交易成本模拟 |
| 风险控制 | 个股止损、组合回撤熔断、滑点模拟 |
| 调仓频率 | 支持日频和周频调仓 |
| 分析报告 | 自动生成绩效报告、IC分析、可视化图表 |
| 自主实验 | 自动搜索超参数空间，保留最优策略 |

---

## 环境安装

### 前提条件

- Python 3.10+
- uv 包管理器（推荐）或 pip

### 安装步骤

```bash
# 进入项目目录
cd autoresearch

# 方式一：使用 uv 安装（推荐）
uv pip install -r requirements_quant.txt

# 方式二：使用 pip 安装
pip install -r requirements_quant.txt
```

### 依赖清单

| 包名 | 用途 |
|------|------|
| yfinance | 获取美股行情数据 |
| lightgbm | 梯度提升树模型 |
| scikit-learn | 线性模型、评估指标 |
| torch | 深度学习（LSTM/GRU） |
| pandas / numpy | 数据处理 |
| matplotlib / seaborn | 可视化 |
| scipy | 统计分析 |
| tqdm | 进度条 |
| pyarrow | Parquet 文件读写 |

---

## 快速开始

### 一键运行完整流程

```bash
# 使用10只股票快速测试（约5秒完成）
python run_pipeline.py --step all --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 使用全部纳斯达克100成分股（首次运行需下载数据，约10分钟）
python run_pipeline.py --step all
```

### 分步运行

```bash
# 第1步：下载数据 + 构建因子
python run_pipeline.py --step data

# 第2步：训练模型
python run_pipeline.py --step train

# 第3步：策略回测
python run_pipeline.py --step backtest

# 第4步：生成分析报告
python run_pipeline.py --step analysis
```

运行完成后，所有结果保存在 `quant_results/` 目录下。

---

## 功能模块详解

### 1. 数据采集与处理

**功能说明：**
- 从 Yahoo Finance 下载指定股票的 OHLCV（开盘价、最高价、最低价、收盘价、成交量）日线数据
- 下载纳斯达克100指数（NDX）基准数据
- 计算 Alpha158 因子体系（142个技术因子）
- 生成 Qlib 格式的数据文件

**使用方式：**

```bash
# 下载默认92只纳斯达克100成分股
python run_pipeline.py --step data

# 指定股票列表
python run_pipeline.py --step data --symbols "AAPL,MSFT,GOOGL"
```

**数据存储位置：**
- 原始CSV数据：`~/.qlib/csv_data/nasdaq/`
- 特征数据集：`quant_results/dataset.parquet`
- Qlib格式数据：`~/.qlib/qlib_data/nasdaq_data/`

**Alpha158 因子分类：**

| 类别 | 因子 | 窗口期 |
|------|------|--------|
| 价格形态 | KMID, KLEN, KUP, KLOW, KSFT | 当日 |
| 价格相对 | OPEN, HIGH, LOW, CLOSE | 5/10/20/30/60 |
| 成交量 | VOLUME | 5/10/20/30/60 |
| 移动平均 | MA | 5/10/20/30/60 |
| 标准差 | STD | 5/10/20/30/60 |
| 动量 | ROC | 5/10/20/30/60 |
| 最值 | MAX, MIN | 5/10/20/30/60 |
| 分位数 | QTLU, QTLD | 5/10/20/30/60 |
| 排名 | RANK | 5/10 |
| 相对强弱 | RSV | 5/10/20/30/60 |
| 相关性 | CORR, CORD | 5/10/20 |
| 涨跌统计 | CNTP, CNTN, CNTD | 5/10/20/30/60 |
| 涨跌幅 | SUMP, SUMN, SUMD | 5/10/20/30/60 |
| 量价 | VMA, VSTD, WVMA | 5/10/20/30/60 |
| BETA/R² | BETA, RSQR, RESI | 5/10/20 |
| 高阶统计 | SKEW, KURT | 5/10/20 |

---

### 2. 模型训练

**支持的模型类型：**

| 模型 | 参数名 | 特点 | 适用场景 |
|------|--------|------|---------|
| LightGBM | `lightgbm` | 梯度提升树，训练速度快 | 默认推荐，CPU即可 |
| Ridge | `linear` | 岭回归，简单基线 | 快速验证 |
| LSTM | `lstm` | 长短期记忆网络 | 序列特征建模（需GPU） |
| GRU | `gru` | 门控循环单元 | 轻量级序列模型 |

**使用方式：**

```bash
# 使用 LightGBM（默认）
python run_pipeline.py --step train --model lightgbm

# 使用 Ridge 回归
python run_pipeline.py --step train --model linear

# 使用 LSTM
python run_pipeline.py --step train --model lstm

# 使用 GRU
python run_pipeline.py --step train --model gru
```

**LightGBM 超参数调优：**

```bash
python run_pipeline.py --step train --model lightgbm \
    --learning-rate 0.05 \
    --max-depth 6 \
    --num-leaves 64 \
    --lambda-l1 1.0 \
    --lambda-l2 1.0
```

**训练输出指标：**
- `train_mse`：训练集均方误差
- `valid_mse`：验证集均方误差
- `train_ic`：训练集信息系数
- `valid_ic`：验证集信息系数

**时间划分（默认）：**

| 数据集 | 时间范围 | 用途 |
|--------|---------|------|
| 训练集 | 2015-01-01 ~ 2021-12-31 | 模型训练 |
| 验证集 | 2022-01-01 ~ 2022-12-31 | 超参数调优 / 早停 |
| 测试集 | 2023-01-01 ~ 2024-12-31 | 最终回测评估 |

---

### 3. 策略回测

**策略说明：TopK-Dropout**

每个交易日：
1. 根据模型预测分数对所有股票排序
2. 持有预测分数最高的前 K 只股票
3. 每次调仓最多卖出 n_drop 只跌出 Top-K 的股票
4. 用空出的资金买入排名最高的新股票
5. 等权分配仓位

**使用方式：**

```bash
# 默认参数回测
python run_pipeline.py --step backtest

# 调整持仓数量和换仓数量
python run_pipeline.py --step backtest --topk 20 --n-drop 3

# 启用风控（止损 + 回撤熔断）
python run_pipeline.py --step backtest --stop-loss -0.05 --max-drawdown-limit -0.15

# 周频调仓（降低换手率）
python run_pipeline.py --step backtest --rebalance-freq week
```

**回测输出指标：**

| 指标 | 说明 | 优秀标准 |
|------|------|---------|
| total_return | 总收益率 | — |
| annualized_return | 年化收益率 | > 15% |
| sharpe_ratio | 夏普比率（风险调整收益） | > 1.5 |
| max_drawdown | 最大回撤 | > -20% |
| calmar_ratio | 卡尔玛比率 | > 1.0 |
| win_rate | 胜率 | > 50% |
| avg_turnover | 日均换手率 | < 30% |
| stop_loss_events | 止损触发次数 | — |
| circuit_breaker_days | 熔断天数 | 0 |

---

### 4. 分析报告

**功能说明：**
- 生成完整的绩效分析文本报告
- IC（信息系数）分析：衡量预测信号质量
- 分组收益分析：将股票按预测分数分5组，计算各组收益
- 可视化图表：累计收益曲线、回撤图、收益分布、滚动夏普等

**使用方式：**

```bash
python run_pipeline.py --step analysis
```

**输出文件：**

| 文件 | 说明 |
|------|------|
| `reports/performance_report.txt` | 完整绩效文本报告 |
| `reports/analysis_report.png` | 6联分析图表（累计收益、回撤、收益分布、滚动夏普、IC、换手率） |
| `reports/equity_curve.png` | 组合净值曲线图 |
| `reports/ic_analysis.csv` | IC时间序列数据 |
| `reports/group_analysis.csv` | 分组收益数据 |
| `reports/feature_importance.csv` | 特征重要性排序（LightGBM） |

---

### 5. 模型集成

**功能说明：**

将多个模型的预测结果加权组合，以提高预测稳健性。

**支持的集成方法：**

| 方法 | 参数名 | 说明 |
|------|--------|------|
| 等权平均 | `equal` | 每个模型权重相同 |
| IC加权 | `ic_weighted` | 根据验证集IC值加权，IC越高权重越大 |
| 排名平均 | `rank_average` | 对预测排名取平均，而非原始分数 |

**使用方式：**

```bash
# 运行 LightGBM + Ridge 的 IC 加权集成
python run_pipeline.py --step ensemble --ensemble --ensemble-method ic_weighted

# 运行集成后接回测和分析
python run_pipeline.py --step ensemble --ensemble
python run_pipeline.py --step backtest
python run_pipeline.py --step analysis
```

**集成输出：**
- 每个子模型的训练指标
- 集成权重分配
- 集成后的预测分数

---

### 6. 自主实验循环

**功能说明：**

借鉴 autoresearch 的自主实验范式，自动搜索最优策略参数：

```
读取历史实验结果 → 随机变异参数 → 运行回测 → 比较夏普比率
→ 改善则保留(keep)，否则丢弃(discard) → 记录日志 → 循环
```

**使用方式：**

```bash
# 运行20轮自动实验
python autorun.py --max-iter 20 --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 无限循环（Ctrl+C 停止）
python autorun.py --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA"

# 设置随机种子（可复现）
python autorun.py --max-iter 10 --seed 42
```

**自动搜索的参数空间：**

| 参数 | 搜索范围 |
|------|---------|
| 模型类型 | lightgbm, linear |
| topk | 5, 10, 15, 20, 30 |
| n_drop | 1, 2, 3, 5, 8 |
| 学习率 | 0.005, 0.01, 0.02, 0.05, 0.1 |
| 最大深度 | 4, 5, 6, 7, 8 |
| 叶子节点数 | 31, 48, 64, 96, 128 |
| L1正则化 | 0.0, 0.1, 0.5, 1.0, 5.0 |
| L2正则化 | 0.0, 0.1, 0.5, 1.0, 5.0 |
| 止损线 | -5%, -8%, -10%, -15%, 无 |
| 调仓频率 | 日频, 周频 |

**决策规则：**
- 新实验 `sharpe_ratio` > 历史最优 → **keep**（保留）
- 新实验 `sharpe_ratio` ≤ 历史最优 → **discard**（丢弃）
- `max_drawdown` 超过 -30% → **crash**（硬约束违反）

**实验日志：**

自动保存到 `quant_results/autorun_results.tsv`，格式如下：

```
timestamp           sharpe    max_dd    annual_ret  status   model      description
2026-03-15 21:42    1.8365    -0.1447   0.4853      keep     lightgbm   topk=5 lr=0.02
2026-03-15 21:42    1.8228    -0.1747   0.4792      discard  lightgbm   topk=5 lr=0.005
2026-03-15 21:42    2.1853    -0.2009   0.6051      keep     linear     topk=5 freq=week
```

---

## 风控功能

### 个股止损

当单只股票当日跌幅超过止损阈值时，自动从组合中移除该股票。

```bash
# 设置止损线为 -5%
python run_pipeline.py --step backtest --stop-loss -0.05

# 设置止损线为 -10%
python run_pipeline.py --step backtest --stop-loss -0.10

# 关闭止损（设置极小值）
python run_pipeline.py --step backtest --stop-loss -1.0
```

**止损逻辑：**
1. 每日检查持仓中每只股票的当日收益率
2. 如果某只股票当日跌幅 ≤ 止损阈值 → 从组合中移除
3. 移除后自动重新分配剩余持仓的权重
4. 触发次数记录在回测指标 `stop_loss_events` 中

### 组合回撤熔断

当组合整体回撤超过限制时，触发熔断机制，暂停交易。

```bash
# 回撤超过15%时触发熔断
python run_pipeline.py --step backtest --max-drawdown-limit -0.15

# 回撤超过20%时触发熔断（默认）
python run_pipeline.py --step backtest --max-drawdown-limit -0.20
```

**熔断逻辑：**
1. 实时跟踪组合累计收益的历史最高点（peak）
2. 当 (当前收益 - peak) / peak ≤ 回撤限制 → 触发熔断
3. 熔断期间：平仓，不交易
4. 冷却 10 个交易日后自动恢复交易
5. 熔断天数记录在回测指标 `circuit_breaker_days` 中

### 滑点模拟

交易时自动模拟市场滑点成本（默认 0.1%）。

交易总成本 = 买入佣金 + 卖出佣金 + 滑点 = 0.1% + 0.1% + 0.1% = 0.3%（单边）

---

## 周频调仓

默认每日调仓，可切换为每周只在周一（或该周第一个交易日）调仓，显著降低换手率和交易成本。

```bash
# 周频调仓
python run_pipeline.py --step backtest --rebalance-freq week

# 日频调仓（默认）
python run_pipeline.py --step backtest --rebalance-freq day
```

**效果对比（10只股票测试）：**

| 调仓频率 | 夏普比率 | 最大回撤 | 日均换手率 |
|---------|---------|---------|----------|
| 日频 | 4.44 | -9.26% | 18.65% |
| 周频 | 4.11 | -13.80% | 5.42% |

> 周频调仓换手率降低约 70%，适合交易成本敏感的场景。

---

## 完整参数列表

### run_pipeline.py 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--step` | `all` | 流程步骤：`all`, `data`, `train`, `backtest`, `analysis`, `ensemble` |
| `--model` | `lightgbm` | 模型类型：`lightgbm`, `linear`, `lstm`, `gru` |
| `--topk` | `30` | TopK 策略持仓股票数 |
| `--n-drop` | `5` | 每次调仓最多卖出的股票数 |
| `--symbols` | 纳斯达克100 | 自定义股票列表，逗号分隔 |
| `--test-start` | `2023-01-01` | 测试期开始日期 |
| `--test-end` | `2024-12-31` | 测试期结束日期 |
| `--learning-rate` | `0.0421` | LightGBM 学习率 |
| `--max-depth` | `6` | LightGBM 最大树深 |
| `--num-leaves` | `64` | LightGBM 叶子节点数 |
| `--lambda-l1` | `1.0` | LightGBM L1 正则化 |
| `--lambda-l2` | `1.0` | LightGBM L2 正则化 |
| `--stop-loss` | `-0.08` | 个股止损阈值（-8%） |
| `--max-drawdown-limit` | `-0.20` | 组合回撤熔断阈值（-20%） |
| `--rebalance-freq` | `day` | 调仓频率：`day` 或 `week` |
| `--ensemble` | `false` | 是否启用模型集成 |
| `--ensemble-method` | `ic_weighted` | 集成方法：`equal`, `ic_weighted`, `rank_average` |

### autorun.py 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--max-iter` | `0` | 最大实验次数（0 = 无限循环） |
| `--symbols` | `None` | 自定义股票列表 |
| `--seed` | `None` | 随机种子 |

---

## 输出文件说明

所有输出保存在 `quant_results/` 目录下：

```
quant_results/
├── dataset.parquet                # Alpha158 特征数据集
├── predictions.parquet            # 模型预测分数
├── backtest_result.pkl            # 回测结果（含日级明细）
├── results.tsv                    # 模型训练日志
├── autorun_results.tsv            # 自主实验日志
├── models/
│   ├── lightgbm_model.pkl         # LightGBM 模型文件
│   ├── ensemble_lightgbm_0.pkl    # 集成子模型
│   ├── ensemble_linear_1.pkl
│   └── ensemble_config.json       # 集成配置
└── reports/
    ├── performance_report.txt     # 绩效文本报告
    ├── analysis_report.png        # 6联分析图表
    ├── equity_curve.png           # 净值曲线图
    ├── ic_analysis.csv            # IC 时间序列
    ├── group_analysis.csv         # 分组收益
    └── feature_importance.csv     # 特征重要性（LightGBM）
```

---

## 常见问题

### Q1: 首次运行报错 "No data found"

**原因：** 数据尚未下载。
**解决：** 先运行数据步骤：
```bash
python run_pipeline.py --step data --symbols "AAPL,MSFT"
```

### Q2: scipy 或 scikit-learn 导入报错

**原因：** Python 3.10 环境下某些版本不兼容。
**解决：**
```bash
uv pip install "scipy<1.15" "scikit-learn<1.6"
```

### Q3: 模型不学习（所有特征重要性为0）

**原因：** LightGBM 正则化参数过大或股票数量太少。
**解决：** 降低 `lambda_l1` 和 `lambda_l2`：
```bash
python run_pipeline.py --step train --lambda-l1 0.5 --lambda-l2 0.5
```

### Q4: 回测中 topk 大于股票数量

**原因：** 指定的 `--topk` 大于实际可用的股票数。
**解决：** 系统会自动调整 topk，无需手动处理。

### Q5: 如何只回测而不重新训练？

```bash
# 只运行回测（使用上次训练的预测结果）
python run_pipeline.py --step backtest

# 只生成报告
python run_pipeline.py --step analysis
```

### Q6: 如何查看自主实验的历史记录？

```bash
# 查看实验日志
cat quant_results/autorun_results.tsv

# 查看最佳结果
sort -t$'\t' -k2 -rn quant_results/autorun_results.tsv | head -5
```

### Q7: 系统支持实盘交易吗？

目前系统仅支持历史回测，不支持实盘交易。未来计划对接 Interactive Brokers API。

> **⚠️ 免责声明：** 本系统仅供研究学习使用，历史回测不代表未来表现，不构成任何投资建议。

---

## 项目文件结构

```
autoresearch/
├── nasdaq_quant/                  # 核心量化系统包
│   ├── __init__.py
│   ├── config.py                  # 全局配置（模型/策略/风控参数）
│   ├── data/
│   │   ├── collector.py           # Yahoo Finance 数据采集器
│   │   └── processor.py           # Alpha158 因子构建 & 数据处理
│   ├── models/
│   │   ├── trainer.py             # 模型训练器（LightGBM/Ridge/LSTM/GRU）
│   │   └── ensemble.py            # 模型集成模块
│   ├── strategy/
│   │   └── backtest.py            # TopK-Dropout 策略 & 回测引擎
│   └── analysis/
│       └── report.py              # 分析报告 & 可视化
├── run_pipeline.py                # 主运行脚本（全流程管线）
├── autorun.py                     # 自主实验循环脚本
├── requirements_quant.txt         # 依赖清单
├── 技术方案/
│   ├── 纳斯达克量化交易系统技术方案.md  # 技术方案文档
│   └── nasdaq-quant-plan.md       # 参考方案
└── quant_results/                 # 运行结果输出目录
    ├── models/                    # 模型文件
    └── reports/                   # 分析报告
```
