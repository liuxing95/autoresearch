# 纳斯达克量化交易系统 — 使用说明

## 目录

- [系统简介](#系统简介)
- [环境安装](#环境安装)
- [快速开始](#快速开始)
- [功能模块详解](#功能模块详解)
  - [1. 数据采集与处理](#1-数据采集与处理)
  - [2. 模型训练](#2-模型训练)
  - [3. 滚动前瞻训练（重要改进）](#3-滚动前瞻训练重要改进)
  - [4. IC 特征筛选](#4-ic-特征筛选)
  - [5. 策略回测](#5-策略回测)
  - [6. 分析报告](#6-分析报告)
  - [7. 模型集成](#7-模型集成)
  - [8. 自主实验循环 v2](#8-自主实验循环-v2)
- [风控功能](#风控功能)
  - [个股止损](#个股止损)
  - [组合回撤熔断](#组合回撤熔断)
  - [滑点模拟](#滑点模拟)
- [周频调仓](#周频调仓)
- [性能对比：静态训练 vs 滚动训练](#性能对比静态训练-vs-滚动训练)
- [最佳实践与调参指南](#最佳实践与调参指南)
- [完整参数列表](#完整参数列表)
- [输出文件说明](#输出文件说明)
- [常见问题](#常见问题)

---

## 系统简介

本系统是一个面向美股纳斯达克100指数成分股的量化交易系统，借鉴了 [autoresearch](https://github.com/karpathy/autoresearch) 的自主实验迭代理念和微软 [Qlib](https://github.com/microsoft/qlib) 的量化投资框架设计。

**核心能力：**

| 功能 | 说明 |
|------|------|
| 数据采集 | 自动从 Yahoo Finance 下载纳斯达克100成分股行情数据 |
| 因子工程 | 内置 Alpha158 因子体系（142个特征因子） |
| 模型训练 | 支持 LightGBM、Ridge、LSTM、GRU 四种模型 |
| **滚动前瞻训练** | **定期用最新数据重新训练模型，适应市场结构变化（关键改进）** |
| **IC 特征筛选** | **自动筛选预测稳定性最高的特征，减少过拟合** |
| 模型集成 | 支持多模型加权集成（等权/IC加权/排名平均） |
| 策略回测 | TopK-Dropout 选股策略，含风控与交易成本模拟 |
| 风险控制 | 个股止损、组合回撤熔断、滑点模拟 |
| 调仓频率 | 支持日频和周频调仓 |
| 分析报告 | 自动生成绩效报告、IC分析、可视化图表 |
| **自主实验 v2** | **多周期鲁棒性评估 + 复合评分，自动搜索最优策略** |

---

## 环境安装

### 前提条件

- Python 3.10+
- [uv](https://docs.astral.sh/uv/) 包管理器

### 安装步骤

```bash
# 进入项目目录
cd autoresearch

# 安装依赖
uv pip install -r requirements_quant.txt
```

> **提示：** 所有 Python 命令均通过 `uv run python` 执行，uv 会自动管理虚拟环境和依赖。

### 依赖清单

| 包名 | 用途 |
|------|------|
| yfinance | 获取美股行情数据 |
| lightgbm | 梯度提升树模型 |
| scikit-learn | 线性模型、评估指标 |
| torch | 深度学习（LSTM/GRU） |
| pandas / numpy | 数据处理 |
| matplotlib / seaborn | 可视化 |
| scipy | 统计分析 |
| tqdm | 进度条 |
| pyarrow | Parquet 文件读写 |

---

## 快速开始

### 一键运行完整流程（静态训练模式）

```bash
# 使用10只股票快速测试（约5秒完成）
uv run python run_pipeline.py --step all --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 使用全部纳斯达克100成分股（首次运行需下载数据，约10分钟）
uv run python run_pipeline.py --step all
```

### 一键运行（推荐：滚动训练模式）

> **为什么推荐滚动训练？** 静态训练只用 2015-2021 的数据训练一次模型，市场结构变化后模型可能完全失效（例如 2025 年夏普比率从正变负）。滚动训练定期用最新数据重新训练模型，让模型始终跟上市场变化。

```bash
# 推荐命令：滚动训练 + 特征筛选（约20秒完成）
uv run python run_pipeline.py --step all \
    --model linear \
    --rolling --train-window 2 --retrain-freq 12 \
    --feature-select --n-features 50 \
    --topk 10 --n-drop 5 \
    --stop-loss -0.05 --max-drawdown-limit -0.50 \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 测试 2025 年的市场表现
uv run python run_pipeline.py --step all \
    --model linear --rolling --train-window 2 --retrain-freq 12 \
    --topk 10 --n-drop 5 --stop-loss -0.05 --max-drawdown-limit -0.50 \
    --test-start "2025-01-01" --test-end "2025-12-31" \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"
```

### 自动搜索最优策略（推荐：让 AI 自己找最好的参数）

```bash
# 运行20轮自动实验，使用多周期鲁棒性评估
uv run python autorun.py --max-iter 20 \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE" \
    --mode robust
```

### 分步运行

```bash
# 第1步：下载数据 + 构建因子
uv run python run_pipeline.py --step data

# 第2步：训练模型（二选一）
uv run python run_pipeline.py --step train              # 静态训练
uv run python run_pipeline.py --step rolling_train --rolling  # 滚动训练（推荐）

# 第3步：策略回测
uv run python run_pipeline.py --step backtest

# 第4步：生成分析报告
uv run python run_pipeline.py --step analysis
```

运行完成后，所有结果保存在 `quant_results/` 目录下。

---

## 功能模块详解

### 1. 数据采集与处理

**功能说明：**
- 从 Yahoo Finance 下载指定股票的 OHLCV（开盘价、最高价、最低价、收盘价、成交量）日线数据
- 下载纳斯达克100指数（NDX）基准数据
- 计算 Alpha158 因子体系（142个技术因子）
- 生成 Qlib 格式的数据文件

**使用方式：**

```bash
# 下载默认92只纳斯达克100成分股
uv run python run_pipeline.py --step data

# 指定股票列表
uv run python run_pipeline.py --step data --symbols "AAPL,MSFT,GOOGL"
```

**数据存储位置：**
- 原始CSV数据：`~/.qlib/csv_data/nasdaq/`
- 特征数据集：`quant_results/dataset.parquet`
- Qlib格式数据：`~/.qlib/qlib_data/nasdaq_data/`

**Alpha158 因子分类：**

| 类别 | 因子 | 窗口期 |
|------|------|--------|
| 价格形态 | KMID, KLEN, KUP, KLOW, KSFT | 当日 |
| 价格相对 | OPEN, HIGH, LOW, CLOSE | 5/10/20/30/60 |
| 成交量 | VOLUME | 5/10/20/30/60 |
| 移动平均 | MA | 5/10/20/30/60 |
| 标准差 | STD | 5/10/20/30/60 |
| 动量 | ROC | 5/10/20/30/60 |
| 最值 | MAX, MIN | 5/10/20/30/60 |
| 分位数 | QTLU, QTLD | 5/10/20/30/60 |
| 排名 | RANK | 5/10 |
| 相对强弱 | RSV | 5/10/20/30/60 |
| 相关性 | CORR, CORD | 5/10/20 |
| 涨跌统计 | CNTP, CNTN, CNTD | 5/10/20/30/60 |
| 涨跌幅 | SUMP, SUMN, SUMD | 5/10/20/30/60 |
| 量价 | VMA, VSTD, WVMA | 5/10/20/30/60 |
| BETA/R² | BETA, RSQR, RESI | 5/10/20 |
| 高阶统计 | SKEW, KURT | 5/10/20 |

---

### 2. 模型训练

**支持的模型类型：**

| 模型 | 参数名 | 特点 | 适用场景 |
|------|--------|------|---------|
| LightGBM | `lightgbm` | 梯度提升树，训练速度快 | 默认推荐，CPU即可 |
| Ridge | `linear` | 岭回归，简单基线 | 快速验证 |
| LSTM | `lstm` | 长短期记忆网络 | 序列特征建模（需GPU） |
| GRU | `gru` | 门控循环单元 | 轻量级序列模型 |

**使用方式：**

```bash
# 使用 LightGBM（默认）
uv run python run_pipeline.py --step train --model lightgbm

# 使用 Ridge 回归
uv run python run_pipeline.py --step train --model linear

# 使用 LSTM
uv run python run_pipeline.py --step train --model lstm

# 使用 GRU
uv run python run_pipeline.py --step train --model gru
```

**LightGBM 超参数调优：**

```bash
uv run python run_pipeline.py --step train --model lightgbm \
    --learning-rate 0.05 \
    --max-depth 6 \
    --num-leaves 64 \
    --lambda-l1 1.0 \
    --lambda-l2 1.0
```

**训练输出指标：**
- `train_mse`：训练集均方误差
- `valid_mse`：验证集均方误差
- `train_ic`：训练集信息系数
- `valid_ic`：验证集信息系数

**时间划分（默认）：**

| 数据集 | 时间范围 | 用途 |
|--------|---------|------|
| 训练集 | 2015-01-01 ~ 2021-12-31 | 模型训练 |
| 验证集 | 2022-01-01 ~ 2022-12-31 | 超参数调优 / 早停 |
| 测试集 | 2023-01-01 ~ 2024-12-31 | 最终回测评估 |

---

### 3. 滚动前瞻训练（重要改进）

> **这是解决模型过时失效的关键功能。** 如果你发现模型在历史数据上表现好但近期表现差，很可能是因为市场结构已经变化，旧模型不再适用。

#### 为什么需要滚动训练？

**问题：静态训练的致命缺陷**

默认的静态训练用 2015-2021 年数据训练一次模型，然后不再更新。时间一长，市场环境变了，模型就会彻底失效。

举一个我们实测的例子：

| 指标 | 静态训练（2025年回测） | 滚动训练（2025年回测） |
|------|----------------------|----------------------|
| 夏普比率 | **-1.36**（亏损） | **+1.51**（盈利） |
| 总收益率 | -16.20% | **+40.99%** |
| 最大回撤 | -20.77% | -18.69% |
| 胜率 | 12.96% | **56.28%** |
| 熔断天数 | 183天（74%时间停止交易） | **0天** |
| IC（预测能力） | -0.0224（反向预测！） | **+0.0457** |
| 多空对冲收益 | -8.52% | **+50.35%** |

可以看到，静态模型到 2025 年不仅没有预测能力，还是**反向预测**（IC 为负），导致亏损。

#### 滚动训练的工作原理

滚动训练的核心思想是：**定期用最新数据重新训练模型**，让模型不断学习新的市场模式。

```
时间轴示意：
├──训练期(2yr)──┤──验证期──┤──测试期──┤
                                      │滑动→
        ├──训练期(2yr)──┤──验证期──┤──测试期──┤
                                              │滑动→
                ├──训练期(2yr)──┤──验证期──┤──测试期──┤
```

**具体步骤：**
1. 用过去 N 年的数据训练模型（例如过去 2 年）
2. 用接下来几个月的数据验证模型，防止过拟合
3. 用模型对下一段时间做预测
4. 时间窗口往前滑动，重复步骤 1-3
5. 最后把所有窗口的预测结果拼接起来，得到完整的测试期预测

#### 使用方式

```bash
# 基本用法：启用滚动训练
uv run python run_pipeline.py --step all --rolling

# 推荐配置：2年训练窗口 + 每12个月重新训练
uv run python run_pipeline.py --step all \
    --model linear --rolling \
    --train-window 2 --retrain-freq 12

# 完整推荐命令（含风控和特征筛选）
uv run python run_pipeline.py --step all \
    --model linear --rolling \
    --train-window 2 --retrain-freq 12 \
    --feature-select --n-features 50 \
    --topk 10 --n-drop 5 \
    --stop-loss -0.05 --max-drawdown-limit -0.50
```

#### 关键参数说明

| 参数 | 默认值 | 说明 | 推荐值 |
|------|--------|------|--------|
| `--rolling` | 不启用 | 启用滚动训练（加上这个标志即可） | 建议始终开启 |
| `--train-window` | `3` | 训练窗口长度（年） | 2-3 年 |
| `--retrain-freq` | `6` | 重新训练间隔（月） | 6-12 个月 |
| `--label-days` | `2` | 预测收益的计算天数 | 2（默认即可） |

#### 模型选择建议

> **重要发现：** 在股票预测这种低信噪比的场景下，**Ridge 线性回归（`linear`）往往比 LightGBM 效果更好**。

原因：LightGBM 是复杂的树模型，容易在训练数据上过拟合。在量化领域，信号很微弱（信噪比低），复杂模型反而会学到噪声。Ridge 回归加了正则化的线性模型，更不容易过拟合。

我们实测的对比：

| 模型 | 2025年夏普比率 | 2025年总收益率 | 说明 |
|------|---------------|---------------|------|
| LightGBM（静态） | -1.36 | -16.20% | 严重过拟合，完全失效 |
| LightGBM（滚动） | 0.25 | +11.17% | 有改善但仍不理想 |
| **Linear（滚动）** | **1.51** | **+40.99%** | **最佳表现** |

**结论：推荐使用 `--model linear --rolling` 的组合。**

---

### 4. IC 特征筛选

#### 什么是 IC 特征筛选？

Alpha158 因子体系生成了 142 个特征，但并非所有特征都对预测有用。有些特征是噪声，会干扰模型学习。IC 特征筛选自动去掉不稳定的特征，只保留预测能力最强的特征。

#### 筛选原理

**IC（Information Coefficient，信息系数）** 衡量一个特征和未来收益的相关性：
- IC > 0：特征能正向预测收益
- IC ≈ 0：特征没有预测能力
- IC < 0：特征是反向指标

**ICIR（IC Information Ratio）** 衡量 IC 的稳定性：

$$ICIR = \frac{\text{IC 的均值}}{\text{IC 的标准差}}$$

ICIR 越高，说明特征的预测能力越稳定。我们用 |ICIR|（绝对值）排序，选出最稳定的 N 个特征。

#### 使用方式

```bash
# 启用特征筛选，保留前50个最稳定的特征
uv run python run_pipeline.py --step all --rolling --feature-select --n-features 50

# 保留更多特征（80个）
uv run python run_pipeline.py --step all --rolling --feature-select --n-features 80
```

#### 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--feature-select` | 不启用 | 启用 IC-ICIR 特征筛选 |
| `--n-features` | `80` | 保留的特征数量（从142个中选） |

> **提示：** 特征筛选通常和滚动训练一起使用才有效果。单独使用效果不明显。

---

### 5. 策略回测

**策略说明：TopK-Dropout**

每个交易日：
1. 根据模型预测分数对所有股票排序
2. 持有预测分数最高的前 K 只股票
3. 每次调仓最多卖出 n_drop 只跌出 Top-K 的股票
4. 用空出的资金买入排名最高的新股票
5. 等权分配仓位

**使用方式：**

```bash
# 默认参数回测
uv run python run_pipeline.py --step backtest

# 调整持仓数量和换仓数量
uv run python run_pipeline.py --step backtest --topk 20 --n-drop 3

# 启用风控（止损 + 回撤熔断）
uv run python run_pipeline.py --step backtest --stop-loss -0.05 --max-drawdown-limit -0.15

# 周频调仓（降低换手率）
uv run python run_pipeline.py --step backtest --rebalance-freq week
```

**回测输出指标：**

| 指标 | 说明 | 优秀标准 |
|------|------|---------|
| total_return | 总收益率 | — |
| annualized_return | 年化收益率 | > 15% |
| sharpe_ratio | 夏普比率（风险调整收益） | > 1.5 |
| max_drawdown | 最大回撤 | > -20% |
| calmar_ratio | 卡尔玛比率 | > 1.0 |
| win_rate | 胜率 | > 50% |
| avg_turnover | 日均换手率 | < 30% |
| stop_loss_events | 止损触发次数 | — |
| circuit_breaker_days | 熔断天数 | 0 |

---

### 6. 分析报告

**功能说明：**
- 生成完整的绩效分析文本报告
- IC（信息系数）分析：衡量预测信号质量
- 分组收益分析：将股票按预测分数分5组，计算各组收益
- 可视化图表：累计收益曲线、回撤图、收益分布、滚动夏普等

**使用方式：**

```bash
uv run python run_pipeline.py --step analysis
```

**输出文件：**

| 文件 | 说明 |
|------|------|
| `reports/performance_report.txt` | 完整绩效文本报告 |
| `reports/analysis_report.png` | 6联分析图表（累计收益、回撤、收益分布、滚动夏普、IC、换手率） |
| `reports/equity_curve.png` | 组合净值曲线图 |
| `reports/ic_analysis.csv` | IC时间序列数据 |
| `reports/group_analysis.csv` | 分组收益数据 |
| `reports/feature_importance.csv` | 特征重要性排序（LightGBM） |

---

### 7. 模型集成

**功能说明：**

将多个模型的预测结果加权组合，以提高预测稳健性。

**支持的集成方法：**

| 方法 | 参数名 | 说明 |
|------|--------|------|
| 等权平均 | `equal` | 每个模型权重相同 |
| IC加权 | `ic_weighted` | 根据验证集IC值加权，IC越高权重越大 |
| 排名平均 | `rank_average` | 对预测排名取平均，而非原始分数 |

**使用方式：**

```bash
# 运行 LightGBM + Ridge 的 IC 加权集成
uv run python run_pipeline.py --step ensemble --ensemble --ensemble-method ic_weighted

# 运行集成后接回测和分析
uv run python run_pipeline.py --step ensemble --ensemble
uv run python run_pipeline.py --step backtest
uv run python run_pipeline.py --step analysis
```

**集成输出：**
- 每个子模型的训练指标
- 集成权重分配
- 集成后的预测分数

---

### 8. 自主实验循环 v2

> **重大升级！** 自主实验系统已升级到 v2 版（旧版备份在 `autorun_v1.py`）。新版最大改进：使用**多周期鲁棒性评估**替代单纯夏普比率比较。

#### 为什么需要 v2？

旧版 (v1) 只看一个时间段的夏普比率，容易找到「只在特定市场环境好用」的策略。例如 v1 找到的策略在 2023-2024 年夏普比率 3.3，但到 2025 年夏普比率变成 -1.36，完全失效。

v2 同时在 **两个不同时段** 评估策略（默认是 2023-2024 和 2025），用复合评分综合衡量，找到**在不同市场环境下都稳定的策略**。

#### 工作流程

```
读取历史实验结果 → 随机变异参数 → 在多个时段运行回测
→ 计算复合鲁棒性评分 → 评分提升则保留(keep) → 记录日志 → 循环
```

#### 使用方式

```bash
# 推荐：多周期鲁棒性模式（20轮实验）
uv run python autorun.py --max-iter 20 \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE" \
    --mode robust

# 无限循环（Ctrl+C 停止）
uv run python autorun.py --mode robust --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA"

# 传统模式：只看单周期夏普比率（不推荐）
uv run python autorun.py --max-iter 20 --mode sharpe

# 设置随机种子（可复现）
uv run python autorun.py --max-iter 10 --seed 42 --mode robust
```

#### 复合鲁棒性评分公式

v2 的核心是将多个维度综合评分，而不是只看夏普比率：

$$\text{Robustness Score} = w_{\text{sharpe}} \cdot S_{\text{sharpe}} + w_{\text{ic}} \cdot S_{\text{ic}} + w_{\text{dd}} \cdot S_{\text{dd}} + w_{\text{consist}} \cdot S_{\text{consist}} - w_{\text{cb}} \cdot P_{\text{cb}}$$

各分项含义：

| 分项 | 权重 | 说明 |
|------|------|------|
| 夏普分数 $S_{\text{sharpe}}$ | 35% | 两个周期的夏普加权平均（P2 权重 60%，因为更看重近期表现） |
| IC 质量 $S_{\text{ic}}$ | 25% | 两个周期 IC 的平均，衡量预测信号质量 |
| 回撤控制 $S_{\text{dd}}$ | 20% | 两个周期最大回撤的平均，越小越好 |
| 一致性 $S_{\text{consist}}$ | 15% | 两个周期夏普的接近程度，惩罚两个周期差异太大的策略 |
| 熔断惩罚 $P_{\text{cb}}$ | 5% | 如果触发熔断机制则扣分 |

> **简单理解：** 好的策略不仅要夏普高，还要 IC 正（真的有预测能力）、回撤小、在不同时段都有效。

#### 自动搜索的参数空间（v2 扩展版）

| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| 模型类型 | lightgbm, linear | linear 推荐 |
| topk | 5, 10, 15, 20, 30 | 持仓股票数 |
| n_drop | 1, 2, 3, 5, 8 | 每次调仓最多卖出 |
| 学习率 | 0.005 ~ 0.1 | LightGBM 专用 |
| 最大深度 | 4 ~ 8 | LightGBM 专用 |
| 叶子节点数 | 31 ~ 128 | LightGBM 专用 |
| L1/L2 正则化 | 0.0 ~ 5.0 | LightGBM 专用 |
| **滚动训练** | **开/关** | **v2 新增：自动搜索滚动训练配置** |
| **训练窗口** | **2, 3, 4, 5 年** | **v2 新增** |
| **重训练间隔** | **3, 6, 12 个月** | **v2 新增** |
| **特征筛选** | **开/关** | **v2 新增** |
| **特征数量** | **30, 50, 80, 100, 142** | **v2 新增** |
| **最大回撤限制** | **-15% ~ -50%** | **v2 新增：更大搜索范围** |
| 止损线 | -5% ~ -15%, 无 | 个股止损 |
| 调仓频率 | 日频, 周频 | |
| subsample | 0.5 ~ 1.0 | LightGBM 行采样 |
| colsample_bytree | 0.5 ~ 1.0 | LightGBM 列采样 |

#### 决策规则（v2）

| 条件 | 结果 | 说明 |
|------|------|------|
| 复合评分 > 历史最优 | **keep**（保留） | 策略整体更好 |
| 复合评分 ≤ 历史最优 | **discard**（丢弃） | 没有改善 |
| 任一周期 crashed | **crash**（崩溃） | 硬约束违反 |

#### 实验日志格式（v2）

自动保存到 `quant_results/autorun_results.tsv`，9列格式：

```
timestamp           sharpe    max_dd    annual_ret  status   model    description                                robustness  mode
2026-03-15 21:42    1.5100    -0.1869   0.4099      keep     linear   topk=10 n_drop=5 freq=day roll=2yr/12mo    0.828       robust
2026-03-15 21:43    0.2500    -0.1500   0.1117      discard  lightgbm topk=10 n_drop=3 freq=day roll=3yr/6mo    0.312       robust
```

> **提示：** `robustness` 列是 v2 新增的复合评分，范围 0-1，越高越好。大于 0.5 算不错，大于 0.8 非常优秀。

---

## 风控功能

### 个股止损

当单只股票当日跌幅超过止损阈值时，自动从组合中移除该股票。

```bash
# 设置止损线为 -5%
uv run python run_pipeline.py --step backtest --stop-loss -0.05

# 设置止损线为 -10%
uv run python run_pipeline.py --step backtest --stop-loss -0.10

# 关闭止损（设置极小值）
uv run python run_pipeline.py --step backtest --stop-loss -1.0
```

**止损逻辑：**
1. 每日检查持仓中每只股票的当日收益率
2. 如果某只股票当日跌幅 ≤ 止损阈值 → 从组合中移除
3. 移除后自动重新分配剩余持仓的权重
4. 触发次数记录在回测指标 `stop_loss_events` 中

### 组合回撤熔断

当组合整体回撤超过限制时，触发熔断机制，暂停交易。

```bash
# 回撤超过15%时触发熔断
uv run python run_pipeline.py --step backtest --max-drawdown-limit -0.15

# 回撤超过20%时触发熔断（默认）
uv run python run_pipeline.py --step backtest --max-drawdown-limit -0.20
```

**熔断逻辑：**
1. 实时跟踪组合累计收益的历史最高点（peak）
2. 当 (当前收益 - peak) / peak ≤ 回撤限制 → 触发熔断
3. 熔断期间：平仓，不交易
4. 冷却 10 个交易日后自动恢复交易
5. 熔断天数记录在回测指标 `circuit_breaker_days` 中

### 滑点模拟

交易时自动模拟市场滑点成本（默认 0.1%）。

交易总成本 = 买入佣金 + 卖出佣金 + 滑点 = 0.1% + 0.1% + 0.1% = 0.3%（单边）

---

## 周频调仓

默认每日调仓，可切换为每周只在周一（或该周第一个交易日）调仓，显著降低换手率和交易成本。

```bash
# 周频调仓
uv run python run_pipeline.py --step backtest --rebalance-freq week

# 日频调仓（默认）
uv run python run_pipeline.py --step backtest --rebalance-freq day
```

**效果对比（10只股票测试）：**

| 调仓频率 | 夏普比率 | 最大回撤 | 日均换手率 |
|---------|---------|---------|----------|
| 日频 | 4.44 | -9.26% | 18.65% |
| 周频 | 4.11 | -13.80% | 5.42% |

> 周频调仓换手率降低约 70%，适合交易成本敏感的场景。

---

## 性能对比：静态训练 vs 滚动训练

以下是使用 10 只纳斯达克100成分股在不同时期的实测结果，帮助你理解各种方法的效果差异。

### 2023-2024 年（牛市环境）

| 方法 | 模型 | 夏普比率 | 总收益率 | 最大回撤 | IC | 多空收益 |
|------|------|---------|---------|---------|-----|---------|
| 静态训练 | LightGBM | 3.32 | +87.49% | -9.97% | 0.047 | -28.81% |
| 滚动训练 | Linear | **4.03** | **+94.46%** | -10.74% | — | — |

> **注意：** 静态 LightGBM 虽然夏普 3.32，但多空收益为 **-28.81%**（做空组反而涨得更多），说明它只是搭上了牛市的顺风车，真正的选股 alpha 是负的。

### 2025 年（震荡/下跌环境）

| 方法 | 模型 | 夏普比率 | 总收益率 | 最大回撤 | IC | 胜率 | 熔断天数 |
|------|------|---------|---------|---------|-----|------|---------|
| 静态训练 | LightGBM | -1.36 | -16.20% | -20.77% | -0.022 | 12.96% | 183天 |
| 滚动训练 | LightGBM | 0.25 | +11.17% | -15.38% | +0.046 | — | 0天 |
| **滚动训练** | **Linear** | **+1.51** | **+40.99%** | **-18.69%** | **+0.046** | **56.28%** | **0天** |

### 关键结论

1. **滚动训练效果显著**：同样是 LightGBM，从 -1.36 提升到 0.25
2. **Linear + 滚动是最佳组合**：夏普 1.51，收益 +41%
3. **静态训练在新市场环境下危险**：IC 变负意味着反向预测
4. **不要只看牛市表现**：2023-2024 的高夏普可能只是市场 beta 的功劳

---

## 最佳实践与调参指南

### 推荐配置（新手直接用这个）

```bash
# 这是目前实测效果最好的配置
uv run python run_pipeline.py --step all \
    --model linear \
    --rolling --train-window 2 --retrain-freq 12 \
    --topk 10 --n-drop 5 \
    --stop-loss -0.05 \
    --max-drawdown-limit -0.50 \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"
```

**每个参数的含义：**

| 参数 | 值 | 为什么这么设 |
|------|-----|-------------|
| `--model linear` | Ridge 回归 | 不容易过拟合，在低信噪比环境更稳定 |
| `--rolling` | 启用滚动训练 | 让模型适应最新市场环境 |
| `--train-window 2` | 用过去2年数据训练 | 太长会包含过时信息，太短数据不够 |
| `--retrain-freq 12` | 每12个月重新训练 | 平衡稳定性和适应性 |
| `--topk 10` | 持有前10只股票 | 集中持仓，alpha 更强 |
| `--n-drop 5` | 每次最多卖5只 | 防止过度换仓 |
| `--stop-loss -0.05` | 单只股票跌5%就卖 | 限制单票风险 |
| `--max-drawdown-limit -0.50` | 回撤50%才熔断 | 宽松熔断，避免误触发 |

### 调参建议

**如果你想改参数，按这个优先级：**

1. **模型选择**（影响最大）：先试 `linear`，如果不满意再试 `lightgbm`
2. **是否滚动**（影响很大）：**务必开启** `--rolling`
3. **topk 和 n_drop**（影响中等）：topk=5~20，n_drop 约为 topk 的一半
4. **风控参数**（影响中等）：止损 -5%~-15%，回撤限制建议放宽到 -0.50
5. **训练窗口**（影响较小）：2~4年都可以
6. **LightGBM 超参**（影响小，仅 LightGBM 时有效）：学习率、深度等

**常见踩坑提示：**

- ❌ **不要把回撤限制设太紧**（如 -0.15）：会导致频繁熔断，大部分时间停止交易
- ❌ **不要关闭滚动训练测试 2025 年**：静态模型几乎肯定失败
- ❌ **不要对 LightGBM 期望太高**：在量化场景容易过拟合
- ✅ **先用 autorun v2 `--mode robust` 搜索**：比手动调参高效得多

---

## 完整参数列表

### run_pipeline.py 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--step` | `all` | 流程步骤：`all`, `data`, `train`, `rolling_train`, `backtest`, `analysis`, `ensemble` |
| `--model` | `lightgbm` | 模型类型：`lightgbm`, `linear`, `lstm`, `gru` |
| `--topk` | `30` | TopK 策略持仓股票数 |
| `--n-drop` | `5` | 每次调仓最多卖出的股票数 |
| `--symbols` | 纳斯达克100 | 自定义股票列表，逗号分隔 |
| `--test-start` | `2023-01-01` | 测试期开始日期 |
| `--test-end` | `2024-12-31` | 测试期结束日期 |
| `--learning-rate` | `0.0421` | LightGBM 学习率 |
| `--max-depth` | `6` | LightGBM 最大树深 |
| `--num-leaves` | `64` | LightGBM 叶子节点数 |
| `--lambda-l1` | `1.0` | LightGBM L1 正则化 |
| `--lambda-l2` | `1.0` | LightGBM L2 正则化 |
| `--subsample` | `0.8` | LightGBM 行采样比例 |
| `--colsample-bytree` | `0.8` | LightGBM 列采样比例 |
| `--stop-loss` | `-0.08` | 个股止损阈值（-8%） |
| `--max-drawdown-limit` | `-0.20` | 组合回撤熔断阈值（-20%） |
| `--rebalance-freq` | `day` | 调仓频率：`day` 或 `week` |
| **`--rolling`** | **不启用** | **启用滚动前瞻训练（加上这个标志即可）** |
| **`--train-window`** | **`3`** | **滚动训练窗口长度（年）** |
| **`--retrain-freq`** | **`6`** | **重新训练间隔（月）** |
| **`--feature-select`** | **不启用** | **启用 IC-ICIR 特征筛选** |
| **`--n-features`** | **`80`** | **保留的特征数量（从142个中选）** |
| **`--label-days`** | **`2`** | **预测收益的计算天数** |
| `--ensemble` | `false` | 是否启用模型集成 |
| `--ensemble-method` | `ic_weighted` | 集成方法：`equal`, `ic_weighted`, `rank_average` |

### autorun.py 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--max-iter` | `0` | 最大实验次数（0 = 无限循环） |
| `--symbols` | `None` | 自定义股票列表 |
| `--seed` | `None` | 随机种子 |
| **`--mode`** | **`robust`** | **评估模式：`robust`（多周期鲁棒性）或 `sharpe`（单周期夏普）** |

---

## 输出文件说明

所有输出保存在 `quant_results/` 目录下：

```
quant_results/
├── dataset.parquet                # Alpha158 特征数据集
├── predictions.parquet            # 模型预测分数
├── backtest_result.pkl            # 回测结果（含日级明细）
├── results.tsv                    # 模型训练日志
├── autorun_results.tsv            # 自主实验日志
├── models/
│   ├── lightgbm_model.pkl         # LightGBM 模型文件
│   ├── ensemble_lightgbm_0.pkl    # 集成子模型
│   ├── ensemble_linear_1.pkl
│   └── ensemble_config.json       # 集成配置
└── reports/
    ├── performance_report.txt     # 绩效文本报告
    ├── analysis_report.png        # 6联分析图表
    ├── equity_curve.png           # 净值曲线图
    ├── ic_analysis.csv            # IC 时间序列
    ├── group_analysis.csv         # 分组收益
    └── feature_importance.csv     # 特征重要性（LightGBM）
```

---

## 常见问题

### Q1: 首次运行报错 "No data found"

**原因：** 数据尚未下载。
**解决：** 先运行数据步骤：
```bash
uv run python run_pipeline.py --step data --symbols "AAPL,MSFT"
```

### Q2: scipy 或 scikit-learn 导入报错

**原因：** Python 3.10 环境下某些版本不兼容。
**解决：**
```bash
uv pip install "scipy<1.15" "scikit-learn<1.6"
```

### Q3: 模型不学习（所有特征重要性为0）

**原因：** LightGBM 正则化参数过大或股票数量太少。
**解决：** 降低 `lambda_l1` 和 `lambda_l2`：
```bash
uv run python run_pipeline.py --step train --lambda-l1 0.5 --lambda-l2 0.5
```

### Q4: 回测中 topk 大于股票数量

**原因：** 指定的 `--topk` 大于实际可用的股票数。
**解决：** 系统会自动调整 topk，无需手动处理。

### Q5: 如何只回测而不重新训练？

```bash
# 只运行回测（使用上次训练的预测结果）
uv run python run_pipeline.py --step backtest

# 只生成报告
uv run python run_pipeline.py --step analysis
```

### Q6: 如何查看自主实验的历史记录？

```bash
# 查看实验日志
cat quant_results/autorun_results.tsv

# 查看最佳结果
sort -t$'\t' -k2 -rn quant_results/autorun_results.tsv | head -5
```

### Q7: 系统支持实盘交易吗？

目前系统仅支持历史回测，不支持实盘交易。未来计划对接 Interactive Brokers API。

### Q8: 模型在 2025 年完全失效了怎么办？

**原因：** 静态训练的模型只学了 2015-2021 的市场模式，2025 年市场结构已经变了。

**解决步骤：**
```bash
# 1. 切换到滚动训练 + Linear 模型
uv run python run_pipeline.py --step all \
    --model linear --rolling \
    --train-window 2 --retrain-freq 12 \
    --topk 10 --n-drop 5 \
    --stop-loss -0.05 --max-drawdown-limit -0.50 \
    --test-start "2025-01-01" --test-end "2025-12-31" \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 2. 或者用 autorun v2 自动搜索
uv run python autorun.py --max-iter 20 --mode robust \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"
```

### Q9: 滚动训练和静态训练该选哪个？

| 场景 | 推荐 |
|------|------|
| 测试 2023 年之前的历史数据 | 静态训练也可以 |
| 测试 2024 年及以后的数据 | **必须用滚动训练** |
| 做研究论文对比不同方法 | 两种都跑，对比结果 |
| 实际使用/投资参考 | **必须用滚动训练** |

### Q10: 应该选 LightGBM 还是 Linear？

| 维度 | LightGBM | Linear (Ridge) |
|------|----------|----------------|
| 模型复杂度 | 高（容易过拟合） | 低（不容易过拟合） |
| 训练速度 | 快 | 更快 |
| 静态训练效果 | 中等（容易衰退） | 稳定 |
| 滚动训练效果 | 中等 | **最佳** |
| 推荐指数 | ⭐⭐ | ⭐⭐⭐⭐⭐ |

> 简单来说：**选 Linear**。在量化预测这种信噪比极低的场景，简单模型远胜复杂模型。

### Q11: autorun v2 的 robust 模式和 sharpe 模式有什么区别？

| | robust 模式（推荐） | sharpe 模式 |
|-----|-------------------|------------|
| 评估时段 | 2023-2024 + 2025 两个时段 | 只看一个时段 |
| 评分标准 | 综合评分（夏普+IC+回撤+一致性） | 只看夏普比率 |
| 搜索空间 | 包含滚动训练参数 | 不含滚动训练参数 |
| 找到的策略 | 在不同市场环境都有效 | 可能只在特定环境有效 |

> **⚠️ 免责声明：** 本系统仅供研究学习使用，历史回测不代表未来表现，不构成任何投资建议。

---

## 项目文件结构

```
autoresearch/
├── nasdaq_quant/                  # 核心量化系统包
│   ├── __init__.py
│   ├── config.py                  # 全局配置（模型/策略/风控/滚动训练参数）
│   ├── data/
│   │   ├── collector.py           # Yahoo Finance 数据采集器
│   │   └── processor.py           # Alpha158 因子构建 & 数据处理
│   ├── models/
│   │   ├── trainer.py             # 模型训练器（LightGBM/Ridge/LSTM/GRU）
│   │   └── ensemble.py            # 模型集成模块
│   ├── strategy/
│   │   └── backtest.py            # TopK-Dropout 策略 & 回测引擎
│   └── analysis/
│       └── report.py              # 分析报告 & 可视化
├── run_pipeline.py                # 主运行脚本（全流程管线 + 滚动训练）
├── autorun.py                     # 自主实验循环 v2（多周期鲁棒性评估）
├── autorun_v1.py                  # 自主实验循环 v1（旧版备份）
├── requirements_quant.txt         # 依赖清单
├── 使用说明.md                     # 本文件（中文使用指南）
├── 技术方案/
│   ├── 纳斯达克量化交易系统技术方案.md  # 技术方案文档
│   └── nasdaq-quant-plan.md       # 参考方案
└── quant_results/                 # 运行结果输出目录
    ├── dataset.parquet            # Alpha158 特征数据集
    ├── predictions.parquet        # 模型预测分数
    ├── backtest_result.pkl        # 回测结果
    ├── results.tsv                # 模型训练日志
    ├── autorun_results.tsv        # 自主实验日志（v2格式，9列）
    ├── models/                    # 模型文件
    └── reports/                   # 分析报告
        ├── performance_report.txt # 绩效文本报告
        ├── analysis_report.png    # 6联分析图表
        ├── equity_curve.png       # 净值曲线图
        ├── ic_analysis.csv        # IC 时间序列
        ├── group_analysis.csv     # 分组收益
        └── feature_importance.csv # 特征重要性
```
