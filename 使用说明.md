# 纳斯达克量化交易系统 — 使用说明

## 目录

- [系统简介](#系统简介)
- [环境安装](#环境安装)
- [快速开始](#快速开始)
- [模型与训练](#模型与训练)
- [策略与风控](#策略与风控)
- [自主实验 autorun v2](#自主实验-autorun-v2)
- [实验结论（2013-2022 全量回测）](#实验结论2013-2022-全量回测)
- [完整参数列表](#完整参数列表)
- [输出文件说明](#输出文件说明)
- [常见问题](#常见问题)

---

## 系统简介

面向纳斯达克100成分股的量化交易系统，借鉴 [Qlib](https://github.com/microsoft/qlib) 框架设计。

**核心能力：** 自动数据采集（Yahoo Finance）→ Alpha158 因子工程（142个特征）→ 多模型训练（LightGBM/Ridge/LSTM/GRU）→ 滚动前瞻训练 → IC 特征筛选 → TopK-Dropout 选股回测 → 风控（止损/熔断）→ 自动生成分析报告。

---

## 环境安装

```bash
# 前提：Python 3.10+、uv 包管理器
cd autoresearch
uv pip install -r requirements_quant.txt
```

> 所有命令通过 `uv run python` 执行。

---

## 快速开始

### 一键运行

```bash
# 10只股票快速测试（约5秒）
uv run python run_pipeline.py --step all --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 全部纳斯达克100成分股（首次需下载数据，约10分钟）
uv run python run_pipeline.py --step all
```

### 推荐命令（滚动训练 + 特征筛选）

```bash
uv run python run_pipeline.py --step all \
    --model linear --rolling --train-window 2 --retrain-freq 6 \
    --feature-select --n-features 50 \
    --topk 15 --n-drop 3 \
    --stop-loss -0.10 --max-drawdown-limit -0.50
```

### 分步运行

```bash
uv run python run_pipeline.py --step data            # 下载数据 + 构建因子
uv run python run_pipeline.py --step train            # 静态训练
uv run python run_pipeline.py --step rolling_train --rolling  # 滚动训练（推荐）
uv run python run_pipeline.py --step backtest         # 策略回测
uv run python run_pipeline.py --step analysis         # 生成报告
```

### 自动搜索最优策略

```bash
uv run python autorun.py --max-iter 20 --mode robust \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"
```

---

## 模型与训练

### 支持的模型

| 模型 | 参数名 | 特点 | 推荐度 |
|------|--------|------|--------|
| Ridge 回归 | `linear` | 简单稳定，不易过拟合 | ⭐⭐⭐⭐⭐ |
| LightGBM | `lightgbm` | 梯度提升树，训练快 | ⭐⭐ |
| LSTM | `lstm` | 长短期记忆网络 | ⭐⭐ |
| GRU | `gru` | 门控循环单元 | ⭐⭐ |

> **结论：推荐 `--model linear`。** 量化预测信噪比极低，简单模型远胜复杂模型。LightGBM 容易过拟合，LSTM/GRU 表现与 Linear 接近但训练更慢。

### 静态训练 vs 滚动训练

**静态训练**只用固定历史数据训练一次模型，市场结构变化后容易失效。

**滚动训练**定期用最新数据重新训练模型，让模型适应市场变化。核心思想：用过去 N 年数据训练 → 验证 → 预测 → 窗口前移 → 重复。

```bash
# 滚动训练关键参数
--rolling              # 启用滚动训练（必须加）
--train-window 2       # 训练窗口 2 年（推荐 2-3 年）
--retrain-freq 6       # 每 6 个月重新训练（推荐 6-12 个月）
```

### IC 特征筛选

从 142 个 Alpha158 特征中，按 ICIR（信息系数稳定性）排序，只保留预测能力最强的 N 个特征，减少过拟合。

```bash
--feature-select       # 启用特征筛选
--n-features 50        # 保留前 50 个最稳定的特征
```

> 特征筛选需配合滚动训练使用才有效。

### 模型集成

将多个模型预测结果加权组合，支持等权（`equal`）、IC加权（`ic_weighted`）、排名平均（`rank_average`）。

```bash
uv run python run_pipeline.py --step ensemble --ensemble --ensemble-method ic_weighted
```

---

## 策略与风控

### TopK-Dropout 选股策略

每个交易日：按预测分数排序 → 持有前 K 只股票 → 每次最多换 n_drop 只 → 等权分配仓位。

```bash
--topk 15              # 持有前 15 只股票
--n-drop 3             # 每次最多卖 3 只
--rebalance-freq day   # 调仓频率：day（日频）或 week（周频）
```

### 个股止损

当持仓股票当日跌幅超过阈值时自动卖出。

```bash
--stop-loss -0.10      # 单日跌 10% 触发止损
```

### 组合回撤熔断

组合回撤超限时暂停交易，冷却 10 个交易日后恢复。

```bash
--max-drawdown-limit -0.50   # 回撤 50% 才熔断（建议放宽，避免误触发）
```

### 滑点模拟

交易成本 = 买入佣金 0.1% + 卖出佣金 0.1% + 滑点 0.1% = 单边 0.3%。

---

## 自主实验 autorun v2

自动随机搜索参数组合，使用**多周期鲁棒性评估**替代单纯夏普排序，找到在不同市场环境都稳定的策略。

### 复合鲁棒性评分

$$\text{Score} = 0.35 \cdot S_{\text{sharpe}} + 0.25 \cdot S_{\text{ic}} + 0.20 \cdot S_{\text{dd}} + 0.15 \cdot S_{\text{consist}} - 0.05 \cdot P_{\text{cb}}$$

含义：夏普高 + IC 正 + 回撤小 + 不同时段表现一致 + 不触发熔断。

### 使用方式

```bash
# 推荐：多周期鲁棒性模式
uv run python autorun.py --max-iter 20 --mode robust \
    --symbols "AAPL,MSFT,GOOGL,AMZN,NVDA,META,TSLA,AVGO,NFLX,ADBE"

# 无限循环（Ctrl+C 停止）
uv run python autorun.py --mode robust

# 传统单周期夏普模式（不推荐）
uv run python autorun.py --max-iter 20 --mode sharpe
```

日志保存到 `quant_results/autorun_results.tsv`，robustness 列范围 0-1，>0.5 不错，>0.8 优秀。

---

## 实验结论（2013-2022 全量回测）

以下实验使用 **89 只纳斯达克100成分股**，数据期 2013-2022，训练期 2013-2019，验证期 2020，测试期 **2021-2022**。

基准：**NDX 指数 2021-2022 收益 -13.82%**（2021: +28.56%, 2022: -33.71%）。

### 模型对比（8 种配置）

| 模型 | 模式 | 夏普比率 | 总收益率 | 最大回撤 | 多空年化 |
|------|------|---------|---------|---------|---------|
| LightGBM | 静态 | -0.49 | -18.96% | -34.02% | — |
| LightGBM | 滚动 3yr | 0.35 | +31.07% | -33.63% | — |
| **Linear (Ridge)** | **静态** | **0.13** | **+16.35%** | **-21.55%** | — |
| **Linear (Ridge)** | **滚动 3yr** | **0.13** | **+16.39%** | **-17.86%** | **+8.26%** |
| LSTM | 静态 | -0.10 | +2.46% | -26.78% | — |
| LSTM | 滚动 3yr | 0.14 | +13.64% | -22.60% | — |
| GRU | 静态 | 0.13 | +14.69% | -21.73% | — |
| GRU | 滚动 3yr | 0.15 | +14.55% | -24.76% | — |

### 参数调优实验

| 配置 | 夏普 | 收益率 | 最大回撤 | 备注 |
|------|------|--------|---------|------|
| LGB rolling topk=10 sl=-5% | 4.93 | — | — | ❌ 136次止损，指标虚高 |
| LGB rolling topk=10 无止损 | 0.43 | +42.63% | -46.39% | 回撤太大 |
| LGB rolling topk=15 n_drop=3 sl=-8% | 1.19 | +107% | — | ❌ LGB 不稳定 |
| Linear rolling 2yr + FS50 topk=15 sl=-10% | 0.53 | +50.89% | -21.75% | 修复前 |
| **Linear rolling 2yr + FS50 topk=15 sl=-10%（修复后）** | **0.79** | **+73.17%** | **-21.63%** | **✅ 最佳** |

### 最佳配置

```bash
uv run python run_pipeline.py --step all \
    --model linear --rolling --train-window 2 --retrain-freq 6 \
    --feature-select --n-features 50 \
    --topk 15 --n-drop 3 \
    --stop-loss -0.10 --max-drawdown-limit -0.50
```

**最佳结果详情：**

| 指标 | 数值 |
|------|------|
| 总收益率 | +73.17%（NDX 基准 -13.82%，超额 +87%） |
| 年化收益率 | +31.81% |
| 夏普比率 | 0.79 |
| 最大回撤 | -21.63% |
| 卡尔玛比率 | 1.47 |
| 胜率 | 50.50% |
| 多空年化价差 | +14.73% |
| 止损触发 | 31 次 |
| 熔断天数 | 0 天 |
| 终值 | $1,731,720（初始 $1,000,000） |

**分组收益验证：** Top (13.12%) > G2 (16.34%) > G3 (4.55%) > G4 (3.76%) > Bottom (-1.61%)，选股 alpha 有效。

### 核心结论

1. **模型选择：Linear (Ridge) 最优。** LightGBM 容易过拟合且不稳定，LSTM/GRU 无明显优势但训练更慢。
2. **必须启用滚动训练。** 静态 LightGBM 夏普 -0.49，滚动后升至 0.35。Linear 在滚动模式下回撤更小。
3. **IC 特征筛选有效。** 从 142 个特征筛选到 50 个，配合滚动训练效果最好。
4. **止损不宜过紧。** -5% 止损会频繁触发导致指标虚高；-10% 止损较为合理。
5. **回撤限制要放宽。** -15% 会导致频繁熔断；建议设 -50%。
6. **归一化修复提升 49%。** z-score 裁剪至 [-5, 5] 避免了 Ridge 矩阵运算溢出，夏普从 0.53 升至 0.79。

---

## 完整参数列表

### run_pipeline.py

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--step` | `all` | 步骤：`all` / `data` / `train` / `rolling_train` / `backtest` / `analysis` / `ensemble` |
| `--model` | `lightgbm` | 模型：`lightgbm` / `linear` / `lstm` / `gru` |
| `--topk` | `30` | 持仓股票数 |
| `--n-drop` | `5` | 每次最多卖出数 |
| `--symbols` | 纳斯达克100 | 自定义股票列表，逗号分隔 |
| `--test-start` | `2021-01-01` | 测试期开始 |
| `--test-end` | `2022-12-31` | 测试期结束 |
| `--rolling` | 不启用 | 启用滚动训练 |
| `--train-window` | `3` | 训练窗口（年） |
| `--retrain-freq` | `6` | 重训间隔（月） |
| `--feature-select` | 不启用 | 启用 IC 特征筛选 |
| `--n-features` | `80` | 保留特征数 |
| `--label-days` | `2` | 预测收益天数 |
| `--stop-loss` | `-0.08` | 个股止损阈值 |
| `--max-drawdown-limit` | `-0.20` | 回撤熔断阈值 |
| `--rebalance-freq` | `day` | 调仓频率：`day` / `week` |
| `--learning-rate` | `0.0421` | LightGBM 学习率 |
| `--max-depth` | `6` | LightGBM 最大深度 |
| `--num-leaves` | `64` | LightGBM 叶子数 |
| `--lambda-l1` | `1.0` | LightGBM L1 正则 |
| `--lambda-l2` | `1.0` | LightGBM L2 正则 |
| `--subsample` | `0.8` | LightGBM 行采样 |
| `--colsample-bytree` | `0.8` | LightGBM 列采样 |
| `--ensemble` | `false` | 启用模型集成 |
| `--ensemble-method` | `ic_weighted` | 集成方法：`equal` / `ic_weighted` / `rank_average` |

### autorun.py

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--max-iter` | `0` | 最大实验次数（0 = 无限） |
| `--symbols` | `None` | 自定义股票列表 |
| `--seed` | `None` | 随机种子 |
| `--mode` | `robust` | 模式：`robust`（多周期鲁棒性） / `sharpe`（单周期） |

---

## 输出文件说明

```
quant_results/
├── dataset.parquet              # Alpha158 特征数据集
├── predictions.parquet          # 模型预测分数
├── backtest_result.pkl          # 回测结果
├── results.tsv                  # 训练日志
├── autorun_results.tsv          # 自主实验日志
├── models/                      # 模型文件
└── reports/
    ├── performance_report.txt   # 绩效报告
    ├── analysis_report.png      # 6联分析图表
    ├── equity_curve.png         # 净值曲线
    ├── ic_analysis.csv          # IC 时间序列
    ├── group_analysis.csv       # 分组收益
    └── feature_importance.csv   # 特征重要性
```

---

## 常见问题

**Q: 首次运行报错 "No data found"？**
先下载数据：`uv run python run_pipeline.py --step data`

**Q: scipy/scikit-learn 导入报错？**
```bash
uv pip install "scipy<1.15" "scikit-learn<1.6"
```

**Q: 怎么只回测不重新训练？**
```bash
uv run python run_pipeline.py --step backtest    # 用上次的预测结果
uv run python run_pipeline.py --step analysis    # 只生成报告
```

**Q: 模型在近期完全失效？**
切换到滚动训练 + Linear：`--model linear --rolling --train-window 2 --retrain-freq 12`

**Q: 该选 LightGBM 还是 Linear？**
选 **Linear**。量化预测信噪比极低，简单模型更稳定。见上方实验结论。

**Q: autorun 的 robust 和 sharpe 模式区别？**
robust 同时评估多个时段 + 综合评分，找到稳定策略；sharpe 只看单期夏普，容易过拟合。

**Q: 如何查看实验历史？**
```bash
cat quant_results/autorun_results.tsv
sort -t$'\t' -k2 -rn quant_results/autorun_results.tsv | head -5
```

> **⚠️ 免责声明：** 本系统仅供研究学习，历史回测不代表未来表现，不构成投资建议。
